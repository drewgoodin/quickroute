<h4>Welcome to Quickroute!</h4>
<p>This is a bare-bones application to demonstrate the routing mechanism. A user was set up for you upon install (username: admnin, password: quickroute).<p>
<p>Check out ./templates, ./routes/public.pl, and ./routes/auth.pl to see what's going on.</p>
<form method='POST'>
  <p>username: <input type = 'text' name = 'username' /></p>
  <p>password: <input type = 'text' name = 'password' /></p>
  <p>remember me? <input type = 'checkbox' name = 'remember' unchecked /></p>
  <button type='submit'>Log In</button>
</form>

